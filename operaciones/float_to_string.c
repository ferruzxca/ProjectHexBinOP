#include <stdio.h>

void float_to_string(float num, char *str) {
    // Using sprintf for simplicity in demonstration
    sprintf(str, "%.6f", num); // You can use a custom implementation if needed
}
